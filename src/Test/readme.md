# Tests

- Ensure a HotPocket instance is running locally at wss://localhost:8081 with this contract deployed.
- Run tests via root script:

```
npm test
```

or from this folder:

```
npm install
npm test
```
